  <section class="section">
  <%= form_for(@user) do |f| %>
    <% if @user.errors.any? %>
      <div id="error_explanation">
        <h2><%= pluralize(@user.errors.count, "error") %> prohibited this user from being saved:</h2>

        <ul>
        <% @user.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
        </ul>
      </div>
    <% end %>
    <div>
     <% form_tag({:action => 'create'}, :multipart=>true) do %>
      <%= text_field "product", "image" %>
      <% end %>
    </div>

    <div class="field">
      <%= f.label :ciudad %><br>
      <%= f.select :ciudad, [["Catalunya"],["Madrid"],["C. valenciana"]] %>
    </div>

    <div class="field">
      <%= f.label :nombre %><br>
      <%= f.text_field :nombre %>
    </div>

    <div class="field">
      <%= f.label :email %><br>
      <%= f.text_field :email %>
    </div>

    <div class="actions">
      <%= f.submit %>
    </div>


    </section>


<% end %>
